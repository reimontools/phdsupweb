import { Title, Container } from "../../component";
export default function NotFound() {
    return (
        <Container.Basic>
            <Title.Primary>Page not found</Title.Primary>
        </Container.Basic>
    );
};